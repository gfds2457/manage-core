# 目标
完善权限管理功能，确保不同角色的用户只能访问和操作其被授权的资源。做到权限最小化，防止越权访问。

# 权限管理：
1. 角色设置：超级管理员 运营 产品
2. 权限设置：
  - 超级管理员：拥有全部权限  • 
  - 运营：商品增删改查、查看RAG知识库，没有用户角色管理权理。  
  - 产品：只能查看商品、查看RAG，不能删除/新增商品，不能管理账号 
3. 前端后端双重守卫：
  - 前端：路由守卫根据权限放行，只展示对应权限中的组件，并且根据权限禁用按钮。
  - 后端：提取token中的用户id，查询用户权限，每个接口都要设置权限鉴权，拒绝未授权的访问。
  - 无权限请求直接返回403禁止访问，拒绝业务执行。
# 用户账号管理
在用户管理页面，支持新增、维护系统账号；创建账号时，必须为账号分配角色，账号权限跟随所属角色自动生效。

# 额外要求
1. 权限变更实时生效：修改用户角色后，用户重新登录即可加载最新权限
2. 代码需要预留扩展：后续新增角色时，不需要大面积改动原有业务代码
3. 首页最近操作日志模块：增加日志，记录高敏感操作（如账号新增、商品删除），实时更新展示
