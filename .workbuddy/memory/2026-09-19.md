# 2026-09-19

- 完成 manageCode 全栈项目 README 评审：扫描三个子项目（admin-front / admin-mock-backend / admin-ai-backend）源码核实文档准确性。
- 关键发现（README 与代码不符）：①技术栈写 JWT 但实际是自签 mock-token；②写 LangChain+LangGraph 但 AI 后端只用 openai SDK + @langchain/textsplitters + LanceDB + MCP；③MySQL 只存 AI 会话，业务数据是内存态；④无混合检索（只有 query 改写+向量检索）；⑤权限码收口为 write/audit 而非查看/新增/编辑/删除。
- 产出：D:\manageCode\README_润色版.md（补全项目结构、接口文档、FAQ 三节 + 修正技术栈/亮点/运行步骤；原 README.md 未改动）。
- 项目事实速记：端口 AI=3000 业务=3001；.env 在 admin-ai-backend/src/.env（模板 src/.env.example）；Node>=22.12；根 package.json 仅 concurrently，依赖需各子项目单独安装；MySQL 需手动建库 manage_core（服务只建表）。
